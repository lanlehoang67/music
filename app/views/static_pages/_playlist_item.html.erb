 <li>
  <a href="javascript:createNewPlaylistSong(<%= playlist_item.id %>)">
            <% if playlist_item.picture.nil? %>
                   <img src="http://via.placeholder.com/30x40">
            <% else %>
                    <img src="<%= playlist_item.picture %>"/>
            <% end %>
   </a>
            <%= playlist_item.name %>
          </li>
